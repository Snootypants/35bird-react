--- a/src/context/HeroSettingsContext.tsx
+++ b/src/context/HeroSettingsContext.tsx
@@ -1,16 +1,5 @@
 import { useState, useMemo } from 'react';
-import { createContext, useContext } from 'react';
-
-export const HeroSettingsContext = createContext(undefined);
-
-export const useHeroSettings = () => {
-  const context = useContext(HeroSettingsContext);
-  if (!context) {
-    throw new Error('useHeroSettings must be used within a HeroSettingsProvider');
-  }
-  return context;
-};
+import { HeroSettingsContext } from './HeroSettings';
 
 export const HeroSettingsProvider = ({ children }) => {
   const [heroImage, setHeroImage] = useState('/images/hero.png');
